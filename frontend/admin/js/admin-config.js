// Central place to configure the backend API URL for the whole admin panel.
// Change this if the backend is deployed somewhere other than localhost.
const ADMIN_API_BASE = 'http://localhost:5000/api';
